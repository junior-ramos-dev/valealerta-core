{
  "project": "vale-alerta",
  "default_region": "tijucas",
  "regions_dir": "../regions",
  "note": "Bacias ficam em /regions/<id>.json. fetch_hydro.py lê esse pacote (VALEALERTA_REGION sobrescreve o default). O dashboard carrega o mesmo JSON via /regions/."
}
